var name = "Alice";   // Using `var` to declare a function-scoped or globally-scoped variable
let age = 25;         // Using `let` to declare a block-scoped variable
const pi = 3.14;      // Using `const` to declare a block-scoped, read-only constant

var array = new Array(); // Declaring a array can be done in a var let and const type. 
