#include <iostream>
#include <string>   // Include for std::string
#include <vector>   // Include for std::vector

int main() {
    // Variable declarations
    int age = 25;                               // Integer
    char initial = 'A';                         // Character
    double pi = 3.14159;                        // Double-precision floating point
    std::string name = "Alice";                 // String (now correctly using std::string)
    int numbers[10] = {0};                      // Array of integers with size 10, initialized to zeros
    std::vector<int> numVector = {1, 2, 3};     // Vector of integers (now correctly using std::vector)

    return 0;
}
